<nav class="navbar navbar-expand-md navbar-light bg-light">
  <div class="container">
    <%= link_to "Acebook", root_path, class: "navbar-brand" %><small> a social network for busy politicians</small>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarMain" aria-controls="navbarsExample04" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarMain">
      <ul class="navbar-nav mr-auto mt-2 mt-md-0">
      </ul>
      <ul class="navbar-nav">
       <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to "New Post", new_post_path, method: :get, class: "nav-link" %>
        </li>

        <li class="nav-item">
          <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "nav-link" %>
        </li>

        <li class="nav-item dropdown">
            <%= link_to root_path, id: "navbar-dropdown", class: "nav-link dropdown-toggle", data: { toggle: "dropdown" }, aria: { haspopup: true, expanded: false } do %>
              <%= current_user.username %>
            <% end %>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbar-dropdown">
              <%= link_to "Settings", edit_user_registration_path, class: "dropdown-item" %>
              <div class="dropdown-divider"></div>
            </div>
          </li>

        <% else %>
          <li class="nav-item"><%= link_to "Sign Up", new_user_registration_path, class: "btn btn-outline-info" %></li>
          <li class="nav-item"><%= link_to "Sign In", new_user_session_path, class: "btn btn-outline-info" %></li>
        <% end %>

      </ul>
    </div>
  </div>
</nav>
